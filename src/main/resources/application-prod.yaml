spring:
  datasource:
    url: jdbc:postgresql://smb.docklas.com:55432/discodeit
    username: discodeit_user
    password: discodeit1234
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        format_sql: false
    open-in-view: false

  boot:
    admin:
      client:
        url:


server:
  port: 80

logging:
  level:
    com.sprint.mission.discodeit: warn
    org.hiberate.SQL: NONE
    org.hibernate.orm.jdbc.bind: NONE