spring:
  datasource:
    url: jdbc:postgresql://smb.docklas.com:55432/discodeit
    username: discodeit_user
    password: discodeit1234
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

server:
  port: 59080


logging:
  level:
    com.sprint.mission.discodeit: debug
    org.hiberate.SQL: INFO
    org.hibernate.orm.jdbc.bind: INFO